name: tirerack-marker-temp-build
on:
  push:
    branches: [ tmp-tirerack-marker-645 ]
  pull_request:
    branches: [ main ]
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Prepare TireRackConfig source
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/tiresrc
          cp -R .tmp-tire-marker-src/. /tmp/tiresrc/
          test -f /tmp/tiresrc/module.json
          test -f /tmp/tiresrc/TireRackPlugin.cs
          test -f /tmp/tiresrc/OffertSourceBadge.cs
          test -f /tmp/tiresrc/OrderMessageSource.cs
      - name: Checkout Smartstore 6.4.0
        run: git clone --depth 1 --branch 6.4.0 https://github.com/smartstore/Smartstore.git /tmp/smartstore
      - name: Build plugin
        id: build
        shell: bash
        continue-on-error: true
        run: |
          set -uo pipefail
          TARGET=/tmp/smartstore/src/Smartstore.Modules/Gawela.TireRackConfig
          mkdir -p "$TARGET"
          cp -R /tmp/tiresrc/. "$TARGET"/
          dotnet build "$TARGET/Gawela.TireRackConfig.csproj" -c DebugNoRazorCompile -p:RazorCompileOnBuild=false -p:RazorCompileOnPublish=false 2>&1 | tee /tmp/tirerack-marker-build.log
          CODE=${PIPESTATUS[0]}
          echo "$CODE" > /tmp/tirerack-marker-build.exit
          if [ "$CODE" -eq 0 ]; then
            DLL=$(find "$TARGET/bin" -type f -name Gawela.TireRackConfig.dll | head -1)
            test -n "$DLL"
            mkdir -p /tmp/out
            cp "$DLL" /tmp/out/Gawela.TireRackConfig.dll
          fi
          exit "$CODE"
      - name: Upload diagnostics
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: tirerack-marker-build-diagnostics
          path: |
            /tmp/tirerack-marker-build.log
            /tmp/tirerack-marker-build.exit
      - name: Upload DLL
        uses: actions/upload-artifact@v4
        if: steps.build.outcome == 'success'
        with:
          name: tirerackconfig-6.4.5-dll
          path: /tmp/out/Gawela.TireRackConfig.dll
      - name: Fail if compilation failed
        if: steps.build.outcome != 'success'
        run: exit 1
