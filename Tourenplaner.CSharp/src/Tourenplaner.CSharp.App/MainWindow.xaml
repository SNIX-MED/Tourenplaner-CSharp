<Window x:Class="Tourenplaner.CSharp.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:components="clr-namespace:Tourenplaner.CSharp.App.Views.Components"
        xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
        mc:Ignorable="d"
        Title="GAWELA Tourenplaner"
        Width="1800"
        Height="980"
        MinWidth="1220"
        MinHeight="780"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource Brush.WindowBackground}">
    <Window.Resources>
        <CollectionViewSource x:Key="SidebarNavigationItemsView"
                              Source="{Binding SidebarNavigationItems}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="GroupName" />
            </CollectionViewSource.GroupDescriptions>
            <CollectionViewSource.SortDescriptions>
                <scm:SortDescription PropertyName="GroupName" Direction="Ascending" />
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
    </Window.Resources>
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="170" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource FlatCardStyle}" Padding="8" Margin="0,0,8,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="2,6,2,8">
                    <TextBlock FontSize="34" FontWeight="Bold" Text="GAWELA" />
                    <TextBlock Margin="0,-4,0,0" Foreground="{DynamicResource Brush.TextMuted}" Text="Tourenplaner" />
                </StackPanel>

                <StackPanel DockPanel.Dock="Bottom" Margin="0,12,0,0">
                    <Border Style="{StaticResource SoftCardStyle}" Padding="8">
                        <StackPanel>
                            <TextBlock FontWeight="SemiBold" Text="Schnellzugriff" Margin="0,0,0,8" />
                            <Button Content="Route exportieren"
                                    Style="{StaticResource SuccessButtonStyle}"
                                    Margin="0,0,0,6"
                                    Command="{Binding ExportCurrentRouteCommand}" />
                        </StackPanel>
                    </Border>
                    <TextBlock Margin="4,8,0,0" FontSize="11" Foreground="{DynamicResource Brush.TextMuted}" Text="© GAWELA" />
                </StackPanel>

                <ListBox Margin="0,4,0,0"
                         Background="Transparent"
                         BorderThickness="0"
                         ItemsSource="{Binding Source={StaticResource SidebarNavigationItemsView}}"
                         SelectedItem="{Binding SelectedNavigationItem, Mode=TwoWay}"
                         ItemContainerStyle="{StaticResource SidebarListBoxItemStyle}">
                    <ListBox.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock Margin="6,14,6,6"
                                               FontSize="12"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource Brush.TextMuted}"
                                               Text="{Binding Name}" />
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                            <GroupStyle.Panel>
                                <ItemsPanelTemplate>
                                    <StackPanel />
                                </ItemsPanelTemplate>
                            </GroupStyle.Panel>
                        </GroupStyle>
                    </ListBox.GroupStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock FontSize="16" FontWeight="SemiBold" Text="{Binding DisplayName}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <components:DashboardTopBar Grid.Row="0" Margin="0,0,0,8" />

            <Border Grid.Row="1" Style="{StaticResource FlatCardStyle}" Padding="8">
                <ContentControl Content="{Binding CurrentSection}" Style="{StaticResource SectionFadeInContentStyle}" />
            </Border>
        </Grid>
    </Grid>
</Window>

