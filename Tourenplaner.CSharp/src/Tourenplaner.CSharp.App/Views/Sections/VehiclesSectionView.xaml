<UserControl x:Class="Tourenplaner.CSharp.App.Views.Sections.VehiclesSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="650"
             d:DesignWidth="1000">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" FontSize="26" FontWeight="SemiBold" />

        <DockPanel Grid.Row="1" Margin="0,12,0,12">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Width="140" Margin="8,0,0,0" Content="Neu Laden" Command="{Binding RefreshCommand}" />
                <Button Width="130" Margin="8,0,0,0" Content="Speichern" Command="{Binding SaveCommand}" />
                <Button Width="160" Margin="8,0,0,0" Content="Fahrzeug +" Command="{Binding AddVehicleCommand}" />
                <Button Width="150" Margin="8,0,0,0" Content="Fahrzeug -" Command="{Binding RemoveVehicleCommand}" />
                <Button Width="160" Margin="8,0,0,0" Content="Anhänger +" Command="{Binding AddTrailerCommand}" />
                <Button Width="150" Margin="8,0,0,0" Content="Anhänger -" Command="{Binding RemoveTrailerCommand}" />
            </StackPanel>
            <TextBlock VerticalAlignment="Center"
                       FontSize="13"
                       Foreground="#334155"
                       Text="{Binding StatusText}" />
        </DockPanel>

        <GroupBox Grid.Row="2" Header="Fahrzeuge">
            <DataGrid AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      ItemsSource="{Binding Vehicles}"
                      SelectedItem="{Binding SelectedVehicle, Mode=TwoWay}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Width="2*" Binding="{Binding Id, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Typ" Width="90" Binding="{Binding Type, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Name" Width="2*" Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Kennzeichen" Width="120" Binding="{Binding LicensePlate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Nutzlast kg" Width="100" Binding="{Binding MaxPayloadKg, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Anhängelast kg" Width="120" Binding="{Binding MaxTrailerLoadKg, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Volumen m3" Width="90" Binding="{Binding VolumeM3, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridCheckBoxColumn Header="Aktiv" Width="70" Binding="{Binding Active, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Notizen" Width="2*" Binding="{Binding Notes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <GroupBox Grid.Row="3" Margin="0,12,0,0" Header="Anhänger">
            <DataGrid AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      ItemsSource="{Binding Trailers}"
                      SelectedItem="{Binding SelectedTrailer, Mode=TwoWay}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Width="2*" Binding="{Binding Id, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Name" Width="2*" Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Kennzeichen" Width="120" Binding="{Binding LicensePlate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Nutzlast kg" Width="100" Binding="{Binding MaxPayloadKg, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Volumen m3" Width="90" Binding="{Binding VolumeM3, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridCheckBoxColumn Header="Aktiv" Width="70" Binding="{Binding Active, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Notizen" Width="2*" Binding="{Binding Notes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>
