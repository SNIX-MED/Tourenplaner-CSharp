<UserControl x:Class="Tourenplaner.CSharp.App.Views.Sections.EmployeesSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="760"
             d:DesignWidth="1200">
    <Grid Margin="{StaticResource Spacing.PageMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="0,0,0,10" Style="{StaticResource SoftCardStyle}" Padding="10">
            <TextBlock Foreground="{DynamicResource Brush.TextMuted}" Text="{Binding CountText}" />
        </Border>

        <Border Grid.Row="1" Style="{StaticResource EntityListContainerCardStyle}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Entries}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource EntityListItemCardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" FontWeight="SemiBold" FontSize="19" Text="{Binding Name}" />

                                    <Border Grid.Row="0"
                                            Grid.Column="1"
                                            MinWidth="78"
                                            Height="28"
                                            Padding="12,0"
                                            HorizontalAlignment="Right"
                                            CornerRadius="14">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{DynamicResource Brush.DangerSoft}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Active}" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource Brush.SuccessSoft}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource Brush.TextPrimary}"
                                                   Text="{Binding ActiveLabel}" />
                                    </Border>

                                    <TextBlock Grid.Row="1"
                                               Grid.ColumnSpan="2"
                                               Margin="0,10,0,0"
                                               Foreground="{DynamicResource Brush.TextMuted}"
                                               TextWrapping="Wrap"
                                               Text="{Binding DetailsLine}" />

                                    <Grid Grid.Row="2" Grid.ColumnSpan="2" Margin="0,12,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Button Grid.Column="0"
                                                Height="36"
                                                Margin="0,0,8,0"
                                                Content="Bearbeiten"
                                                Style="{StaticResource SecondaryButtonStyle}"
                                                Tag="{Binding}"
                                                Click="OnEditEntryClicked" />
                                        <Button Grid.Column="1"
                                                Height="36"
                                                Margin="8,0,0,0"
                                                Content="Löschen"
                                                Style="{StaticResource DangerOutlineButtonStyle}"
                                                Tag="{Binding}"
                                                Click="OnDeleteEntryClicked" />
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
