<UserControl x:Class="Tourenplaner.CSharp.App.Views.Sections.KalenderSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="760"
             d:DesignWidth="1080">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" FontSize="26" FontWeight="SemiBold" />

        <DockPanel Grid.Row="1" Margin="0,12,0,12">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Width="120" Margin="8,0,0,0" Content="Previous" Command="{Binding PreviousMonthCommand}" />
                <Button Width="120" Margin="8,0,0,0" Content="Next" Command="{Binding NextMonthCommand}" />
                <Button Width="120" Margin="8,0,0,0" Content="Refresh" Command="{Binding RefreshCommand}" />
            </StackPanel>
            <StackPanel Orientation="Vertical">
                <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding DisplayMonthText}" />
                <TextBlock Foreground="#334155" Text="{Binding StatusText}" />
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="2" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="8" Padding="8" Background="#F8FAFC">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <UniformGrid Grid.Row="0" Columns="7" Margin="0,0,0,6">
                    <TextBlock HorizontalAlignment="Center" Text="Mo" FontWeight="SemiBold" />
                    <TextBlock HorizontalAlignment="Center" Text="Di" FontWeight="SemiBold" />
                    <TextBlock HorizontalAlignment="Center" Text="Mi" FontWeight="SemiBold" />
                    <TextBlock HorizontalAlignment="Center" Text="Do" FontWeight="SemiBold" />
                    <TextBlock HorizontalAlignment="Center" Text="Fr" FontWeight="SemiBold" />
                    <TextBlock HorizontalAlignment="Center" Text="Sa" FontWeight="SemiBold" />
                    <TextBlock HorizontalAlignment="Center" Text="So" FontWeight="SemiBold" />
                </UniformGrid>

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding CalendarDays}"
                         SelectedItem="{Binding SelectedDay, Mode=TwoWay}"
                         BorderThickness="0"
                         Background="Transparent">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="7" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="2" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="VerticalContentAlignment" Value="Stretch" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPlaceholder}" Value="True">
                                    <Setter Property="IsHitTestVisible" Value="False" />
                                    <Setter Property="Opacity" Value="0.35" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="4" Padding="6" MinHeight="70">
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" Text="{Binding DayLabel}" />
                                    <TextBlock Margin="0,6,0,0" Foreground="#0F766E">
                                        <Run Text="{Binding TourCount}" />
                                        <Run Text=" Tour(s)" />
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <GroupBox Grid.Row="3" Margin="0,12,0,0" Header="Tours on selected day">
            <DataGrid AutoGenerateColumns="False"
                      IsReadOnly="True"
                      ItemsSource="{Binding SelectedDayTours}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Width="70" Binding="{Binding TourId}" />
                    <DataGridTextColumn Header="Tour" Width="2*" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Start" Width="90" Binding="{Binding StartTime}" />
                    <DataGridTextColumn Header="Vehicle" Width="130" Binding="{Binding VehicleId}" />
                    <DataGridTextColumn Header="Employees" Width="2*" Binding="{Binding Employees}" />
                    <DataGridTextColumn Header="Stops" Width="80" Binding="{Binding StopCount}" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>
