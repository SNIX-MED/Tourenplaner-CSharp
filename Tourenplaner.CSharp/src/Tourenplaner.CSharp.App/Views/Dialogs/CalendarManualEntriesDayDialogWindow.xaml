<Window x:Class="Tourenplaner.CSharp.App.Views.Dialogs.CalendarManualEntriesDayDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tageseinträge"
        Width="620"
        Height="440"
        MinWidth="560"
        MinHeight="380"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.WindowBackground}">
    <Window.Resources>
        <Style x:Key="DayEntryListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="12" Style="{StaticResource FlatCardStyle}" Padding="14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="HeadlineText"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Text="Einträge" />

            <ListBox x:Name="EntriesListBox"
                     Grid.Row="1"
                     Margin="0,12,0,12"
                     BorderThickness="0"
                     Background="Transparent"
                     HorizontalContentAlignment="Stretch"
                     ItemContainerStyle="{StaticResource DayEntryListBoxItemStyle}"
                     MouseDoubleClick="OnEditClickedFromDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border HorizontalAlignment="Stretch">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Margin" Value="0,0,0,8" />
                                    <Setter Property="Padding" Value="10,9" />
                                    <Setter Property="BorderBrush" Value="{DynamicResource Brush.BorderSoft}" />
                                    <Setter Property="BorderThickness" Value="1" />
                                    <Setter Property="CornerRadius" Value="10" />
                                    <Setter Property="Background" Value="{DynamicResource Brush.CardSoft}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsMouseOver}" Value="True">
                                            <Setter Property="BorderBrush" Value="{DynamicResource Brush.BorderStrong}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource Brush.AccentSoft}" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource Brush.Accent}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Width="8"
                                        Height="8"
                                        Margin="4,6,0,0"
                                        CornerRadius="4"
                                        Background="{Binding ColorHex}" />
                                <StackPanel Grid.Column="1" Margin="6,0,0,0">
                                    <TextBlock FontWeight="SemiBold"
                                               Text="{Binding Headline}" />
                                    <TextBlock Margin="0,2,0,0"
                                               FontSize="11"
                                               Foreground="{DynamicResource Brush.TextMuted}"
                                               Text="{Binding KindLabel}" />
                                    <TextBlock Foreground="{DynamicResource Brush.TextMuted}"
                                               Text="{Binding Description}"
                                               TextWrapping="Wrap" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Width="130"
                        HorizontalAlignment="Left"
                        Content="Schliessen"
                        Style="{StaticResource DarkButtonStyle}"
                        IsCancel="True" />

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Width="120"
                            Margin="0,0,8,0"
                            Content="Neu"
                            Style="{StaticResource SuccessButtonStyle}"
                            Click="OnAddClicked" />
                    <Button Width="120"
                            Margin="0,0,8,0"
                            Content="Bearbeiten"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="OnEditClicked" />
                    <Button Width="120"
                            Content="Loeschen"
                            Style="{StaticResource DangerButtonStyle}"
                            Click="OnDeleteClicked" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
