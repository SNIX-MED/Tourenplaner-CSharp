<Window x:Class="Tourenplaner.CSharp.App.Views.Dialogs.ManualOrderDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Kundenkartei"
        Width="1180"
        Height="860"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource Brush.WindowBackground}">
    <Border Margin="12" Style="{StaticResource FlatCardStyle}" Padding="14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock FontSize="34" FontWeight="Bold" Text="Kundenkartei" />
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="240" />
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock FontWeight="SemiBold" Text="Auftragsnummer" />
                        <TextBox Margin="0,4,10,0" Text="{Binding OrderNumber, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock FontWeight="SemiBold" Text="Auftragsdatum (DD.MM.YYYY)" />
                        <TextBox Margin="0,4,10,0" Text="{Binding OrderDateText, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </Grid>
            </StackPanel>

            <ScrollViewer Grid.Row="1" Margin="0,14,0,12" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border Style="{StaticResource SoftCardStyle}" Padding="12" Margin="0,0,0,10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock FontSize="18" FontWeight="SemiBold" Text="Auftragsadresse" />
                            <Grid Grid.Row="1" Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,8">
                                    <TextBlock FontWeight="SemiBold" Text="Name" />
                                    <TextBox Margin="0,4,0,0" Text="{Binding OrderAddressName, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="10,0,0,8">
                                    <TextBlock FontWeight="SemiBold" Text="Strasse" />
                                    <TextBox Margin="0,4,0,0" Text="{Binding OrderAddressStreet, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock FontWeight="SemiBold" Text="PLZ" />
                                    <TextBox Margin="0,4,0,0" Text="{Binding OrderAddressPostalCode, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock FontWeight="SemiBold" Text="Ort" />
                                    <TextBox Margin="0,4,0,0" Text="{Binding OrderAddressCity, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource SoftCardStyle}" Padding="12" Margin="0,0,0,10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock FontSize="18" FontWeight="SemiBold" Text="Lieferadresse" />
                            <Grid Grid.Row="1" Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,8">
                                    <TextBlock FontWeight="SemiBold" Text="Name" />
                                    <TextBox Margin="0,4,0,0" Text="{Binding DeliveryName, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="10,0,0,8">
                                    <TextBlock FontWeight="SemiBold" Text="Person" />
                                    <TextBox Margin="0,4,0,0" Text="{Binding DeliveryContactPerson, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="0,0,0,8">
                                    <TextBlock FontWeight="SemiBold" Text="Strasse" />
                                    <TextBox Margin="0,4,0,0" Text="{Binding DeliveryStreet, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>

                                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock FontWeight="SemiBold" Text="PLZ" />
                                    <TextBox Margin="0,4,0,0" Text="{Binding DeliveryPostalCode, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                                <StackPanel Grid.Row="2" Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock FontWeight="SemiBold" Text="Ort" />
                                    <TextBox Margin="0,4,0,0" Text="{Binding DeliveryCity, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock FontWeight="SemiBold" Text="E-Mail" />
                            <TextBox Margin="0,4,0,0" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock FontWeight="SemiBold" Text="Telefon" />
                            <TextBox Margin="0,4,0,0" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </Grid>

                    <Border Style="{StaticResource SoftCardStyle}" Padding="12" Margin="0,0,0,10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock FontSize="18" FontWeight="SemiBold" Text="Produkte" />

                            <DataGrid Grid.Row="1"
                                      Margin="0,10,0,8"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      ItemsSource="{Binding ProductLines}"
                                      SelectedItem="{Binding SelectedProductLine}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Produkt" Width="*" Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <DataGridTextColumn Header="Gewicht (kg)" Width="160" Binding="{Binding WeightKgText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </DataGrid.Columns>
                            </DataGrid>

                            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="+ Hinzufuegen" Style="{StaticResource SuccessButtonStyle}" Command="{Binding AddProductLineCommand}" />
                                <Button Margin="8,0,0,0" Content="Ausgewaehlte entfernen" Style="{StaticResource DangerButtonStyle}" Command="{Binding RemoveProductLineCommand}" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock FontWeight="SemiBold" Text="Lieferart" />
                            <ComboBox Margin="0,4,0,0" ItemsSource="{Binding DeliveryTypeOptions}" SelectedItem="{Binding SelectedDeliveryType}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock FontWeight="SemiBold" Text="Status" />
                            <ComboBox Margin="0,4,0,0" ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding SelectedStatus}" />
                        </StackPanel>
                    </Grid>

                    <StackPanel>
                        <TextBlock FontWeight="SemiBold" Text="Notizen" />
                        <TextBox Margin="0,4,0,0"
                                 Height="110"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 TextWrapping="Wrap"
                                 Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Schliessen" Style="{StaticResource DarkButtonStyle}" Margin="0,0,8,0" IsCancel="True" />
                <Button Grid.Column="1" Content="Speichern" Style="{StaticResource SuccessButtonStyle}" Margin="8,0,0,0" IsDefault="True" Click="OnSaveClicked" />
            </Grid>
        </Grid>
    </Border>
</Window>
