<Window x:Class="Tourenplaner.CSharp.App.Views.Dialogs.ManualRouteStopDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Stopp hinzufügen"
        Width="430"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource Brush.WindowBackground}">
    <Border Margin="12" Padding="16" Style="{StaticResource FlatCardStyle}">
        <StackPanel>
            <TextBlock x:Name="DialogHeading"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Text="Stopp hinzufügen" />

            <TextBlock Margin="0,16,0,0"
                       FontWeight="SemiBold"
                       Text="Stoppbezeichnung" />
            <TextBox x:Name="StopNameTextBox"
                     Height="36"
                     Margin="0,5,0,0"
                     MaxLength="120" />

            <TextBlock Margin="0,14,0,0"
                       FontWeight="SemiBold"
                       Text="Aufenthaltsdauer" />
            <ComboBox x:Name="StayMinutesComboBox"
                      Height="36"
                      Margin="0,5,0,0"
                      DisplayMemberPath="DisplayText"
                      SelectedValuePath="Value" />

            <TextBlock Margin="0,14,0,0"
                       FontWeight="SemiBold"
                       Text="Notizen" />
            <TextBox x:Name="NotesTextBox"
                     MinHeight="90"
                     Margin="0,5,0,0"
                     Padding="8,5"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalContentAlignment="Top"
                     VerticalScrollBarVisibility="Auto" />

            <Grid Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Height="36"
                        Margin="0,0,7,0"
                        Content="Abbrechen"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="OnCancelClicked" />
                <Button x:Name="SaveButton"
                        Grid.Column="1"
                        Height="36"
                        Margin="7,0,0,0"
                        Content="Stopp hinzufügen"
                        Style="{StaticResource SuccessButtonStyle}"
                        Click="OnAddClicked" />
            </Grid>
        </StackPanel>
    </Border>
</Window>
